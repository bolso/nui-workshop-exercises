import React  from 'react';
import Location from './Location';

export default ({locations}) => (
    <div>
        <ul>
        {/* loop trough locations listt and render each location in <li> */}

        </ul>
    </div>
)

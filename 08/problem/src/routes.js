
//create component that exports Routes collection
//move routes and from Root.js

// define require.esure if not defined ()
if (typeof require.ensure !== "function") require.ensure = function(d, c) {
    c(require)
};

export default (
    <Route ...
)
